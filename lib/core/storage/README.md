# Storage Rules

Use secure storage for:
- access tokens
- refresh tokens
- session identifiers

Use local storage for:
- theme
- locale
- onboarding status
- non-sensitive UI preferences

Do not store sensitive user data in shared preferences unless the project explicitly accepts that tradeoff.
