# Routing Rules

- Keep routes declared in one place.
- Extract redirect logic to guards.
- Do not pass raw `Map<String, dynamic>` between pages when a typed object can be used.
- Keep shell navigation separate from standalone flows.
